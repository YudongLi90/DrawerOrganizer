// Static configuration. Change values here (or via a future deployment-time
// substitution) — the UI does not expose these.
export const CONFIG = {
  // Default divider thickness in mm. Applied to every design created in this build.
  dividerThickness: 10,
};
